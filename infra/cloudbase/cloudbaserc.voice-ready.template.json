{
  "_comments": [
    "PRIVATE_INGRESS_REQUIRED: servicePath=/ serves /admin and /review from the same container.",
    "Do not deploy this template to a public internet hostname without an edge allowlist/VPN for /admin and /review.",
    "Prefer private access gateway or attach nginx with infra/nginx/talk-and-talk.conf.example locks.",
    "maxNum>1 requires RUN_MIGRATE_ON_START=false and a single-flight migrate release job."
  ],
  "PRIVATE_INGRESS_REQUIRED": true,
  "envId": "__REPLACE_WITH_CLOUDBASE_ENV_ID__",
  "framework": {
    "plugins": {
      "talk-and-talk-api": {
        "use": "@cloudbase/framework-plugin-container",
        "inputs": {
          "serviceName": "talk-and-talk-api",
          "servicePath": "/",
          "localPath": "./backend/api",
          "mode": "high-availability",
          "cpu": 1,
          "mem": 2,
          "minNum": 1,
          "maxNum": 5,
          "policyType": "cpu",
          "policyThreshold": 60,
          "containerPort": 3000,
          "dockerfilePath": "./Dockerfile",
          "buildDir": "./"
        }
      }
    }
  }
}
