STAGING_MOCK_PROVIDER_DOES_NOT_READ_THIS_FILE

This is deliberately not a private key. The shared Compose file mounts a key path,
while APP_ENV=staging selects the mock payment provider and never reads it.
