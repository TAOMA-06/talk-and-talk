<view class="action-bar {{fixed ? 'action-bar-fixed' : ''}}">
  <button wx:if="{{secondaryText}}" class="action-secondary" bindtap="secondary">{{secondaryText}}</button>
  <button class="action-primary" disabled="{{disabled}}" loading="{{loading}}" bindtap="primary">{{primaryText}}</button>
</view>
