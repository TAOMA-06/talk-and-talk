# Talk&Talk v0.1 归档说明

日期：2026-09-01

## 远端备份

清理前的 v0.1 状态已保存到远端分支：

```text
codex/v0.1-ui4-backup-20260901
```

该分支停在提交 `6a893e9`，用于回溯清理前的文件布局、历史证据和中间产物。

## 当前主分支

`main` 已更新到清理后的 v0.1 主发行代码：

```text
5c9aa53 chore(release): prune duplicate artifacts
```

这次清理删除了完全重复的 ` 2` 后缀副本和一次性本地 UI 产物，让 `main` 更适合作为主要代码仓库入口。

## 使用边界

备份分支只用于追溯，不作为继续开发或发布的基线。后续功能、修复和发布工作应从 `main` 创建新分支。
