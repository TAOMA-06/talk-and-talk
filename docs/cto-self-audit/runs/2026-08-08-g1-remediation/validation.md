# Phase 0 validation log

> Append-only. This file records commands and results, not release claims.

| Timezone | Evidence | Command / check | Result |
|---|---|---|---|
| Asia/Shanghai | `E0R-BASELINE-20260808` | `git status --short --branch`; `git rev-parse HEAD origin/main` | clean `main...origin/main`; both resolve to `cbac594ba7387e2455faa1df97c489afb5616c07` |
| Asia/Shanghai | `E0R-WRITER-20260808` | local executable capability probe and `hermes status` | no callable Luna writer found; writing gate blocked; no credentials retained in task records |
| Asia/Shanghai | `E0R-AUDIT-INPUT-20260808` | independent Web, Mini/API, and evidence audits from the previous turn | G1 No-Go / G2-ready No-Go; findings entered into `change-map.md` |
| Asia/Shanghai | `E0R-G0-RECONCILE-20260808` | compare prior G0 decision record with the approved delivery guide and the current recovery task | G0-D01 through G0-D04 remain confirmed; stale candidate claims and invalid model waiver were not carried forward |
| Asia/Shanghai | `E0R-SOL-RELEASE-PLAN-20260808` | independent Sol/ultra release and evidence remediation design | isolated E2E, same-SHA runner, artifact, rollback, and executable G2 package requirements recorded; no external action taken |
| Asia/Shanghai | `E0R-SOL-WEB-PLAN-20260808` | independent Sol/ultra Web remediation design | production surface, CTA, allowlist, response-header, CI, and browser-evidence acceptance added to the change map; no business-code change taken |
| Asia/Shanghai | `E0R-SOL-MINI-API-PLAN-20260808` | independent Sol/ultra Mini/API remediation design | isolated E2E guard, text-only legacy-resource closure, preference semantics, field-level OpenAPI checks, identity lifecycle, and earnings-policy dependencies added to the change map; no business-code change taken |
| Asia/Shanghai | `E0R-SOL-IDENTITY-ADR-20260808` | independent Sol/ultra identity authority and interim-boundary ADR | no KYC provider or personal-data collection inferred; decision-ready R01/R02 options and zero-write recovery acceptance prepared |
| Asia/Shanghai | `E0R-SOL-COMMERCIAL-ADR-20260808` | independent Sol/ultra personalization and earnings-policy ADR | decision-ready consent/appeal options, migration constraints, safe projection, and test acceptance prepared |
| Asia/Shanghai | `E0R-SOL-G2-INVENTORY-20260808` | independent Sol/ultra repository-only G2 execution-package inventory | safe repository facts, methods, and external-reference slots populated; no external environment result claimed |
| Asia/Shanghai | `E0R-CANDIDATE-TEMPLATE-20260808` | release-plan implementation of same-SHA evidence and rollback template | template explicitly rejects stale/dirty evidence, required skips, floating artifacts, and unsupported G1/G2 claims |
| Asia/Shanghai | `E0R-BLOCK-AUDIT-20260808` | third consecutive goal-continuation recheck of writer availability, decision register, baseline, and tracked diff | no `luna` executable; model and product authority rows remain pending; current SHA unchanged; task correctly blocked before any business-code write |
| Asia/Shanghai | `E1-SHARED-02A-START-20260809` | user directed model provenance is not a core-work gate; baseline and exclusive lease rechecked before local E2E-safety implementation | `cbac594` baseline recorded; no client lease active; no database, Redis, Docker, or external action run |
| Asia/Shanghai | `E1-SHARED-02A-STATIC-20260809` | `npm run test:e2e:guard`; `npm run assert:e2e:environment` with a synthetic local-run environment; `node --check scripts/run-isolated-e2e.mjs`; `git diff --check` | 34/34 guard tests pass: URL transport overrides, explicit grants, per-run DB identity, pre-claim PostgreSQL/Redis emptiness, dual ownership markers, sealed app/CI runtime values, `.env` and Prisma shadow suppression, all Jest lifecycle hook ownership checks, local Docker endpoint, default Jest isolation, workflow failure and repeated-signal cleanup behavior; synthetic environment accepted; no connection made by the assertion |
| Asia/Shanghai | `E1-SHARED-02A-API-20260809` | `npm run build`; `npm test`; `npm run test:preflight`; manual strict TypeScript check for `test/setup-e2e.ts`; YAML parse for API workflow and E2E Compose | build passed; unit suites 143/143 and tests 1301/1301 passed; preflight 67 pass / 7 pre-existing real-PostgreSQL skips (not treated as G1 pass); setup type check and YAML syntax passed |
| Asia/Shanghai | `E1-SHARED-02A-DENY-20260809` | bare `npm run test:e2e`; `command -v docker` | direct E2E stops at the sealed-runtime check before ownership lookup, Prisma, Jest, cleanup, or any network connection; no Docker executable is installed, so runner runtime proof and real E2E remain pending |
| Asia/Shanghai | `E1-SHARED-02B-LOCAL-20260809` | targeted `npm test -- --runInBand` for review, attendance, support, companion lifecycle, media, conversations, and voice; full `npm test -- --runInBand`; `npm run build`; `node scripts/openapi-controller-contract.test.mjs`; `npm run test:preflight`; `npm run test:e2e:guard`; `git diff --check` | targeted 9 suites / 158 tests passed; full unit suites 143/143 and tests 1313/1313 passed; build passed; OpenAPI 13/13 passed; preflight 68 pass / 7 existing disposable-PostgreSQL skips; E2E guard 34/34 passed; diff check clean. The 7 skips and absent Docker keep G1 runtime evidence blocked. |
| Asia/Shanghai | `E1-SHARED-02B-REVIEW-20260809` | independent static post-fix review of in-scope server paths | static PASS: nonempty evidence IDs fail before transactions/mutations; text-only blocks TRTC callback and client attendance before parsing/DB; legacy chat case title/content/AI/raw evidence is redacted while plaintext conversation review has `attachments=[]`; no additional in-scope P0/P1 found. Provider-managed payment-dispute media and the provenance-unknown human free-text review fields remain explicit non-silent scope boundaries. |
| Asia/Shanghai | `E1-SHARED-02D-LOCAL-20260809` | targeted `npm test -- --runInBand` for refund DTO, payments, orders, and conversations; strict `tsc --noEmit`; `node scripts/openapi-controller-contract.test.mjs`; `npm run build`; full `npm test`; `git diff --check` | targeted 4 suites / 165 tests passed; strict TypeScript passed; OpenAPI 14/14 passed; build passed; full unit suites 144/144 and tests 1325/1325 passed; diff check clean. Customer refunds require trimmed safe 2–200-character reasons and return only the customer projection; cursor pagination is contract-tested. |
| Asia/Shanghai | `E1-SHARED-02D-REVIEW-20260809` | independent contract/refund and owner-boundary static reviews | static PASS: no customer provider/reconciliation field leak; message cursor contract matches runtime; all four owner writes fail closed. Registered E2E now covers customer, other companion owner, third user, and staff; snapshots include order state/timestamps, timeline, audit, notification/outbox, payment, refund, earning, and companion response/completion summary. Docker is absent, so this is not runtime E2E evidence or a G1 claim. |
| Asia/Shanghai | `E1-MINI-02-LOCAL-20260809` | `backend/api/node_modules/.bin/tsc -p frontend/miniprogram/tsconfig.json --noEmit`; Mini `validate.mjs`; release-structure validation with a synthetic valid-format AppID only; Mini `smoke.mjs`; `git diff --check -- frontend/miniprogram` | pass. Mini source/type/release-invariant/smoke gates passed; validate retains only its existing missing external AppID development warning; smoke executed 835 mock API calls. Trial, release, unknown, and environment-query-failure paths are text-only; this is not an AppID upload, device, staging, or production result. |
| Asia/Shanghai | `E1-MINI-02-REVIEW-20260809` | independent source and local-gate review after type and unknown-environment fixes | static/local PASS with no P0/P1: `ChatMessage` and pagination align with OpenAPI; only an explicit Developer Tools `develop` environment may honour a test override; controlled evidence and all in-scope page handlers fail closed in text-only. External AppID, experience build, device, and staging evidence remain pending. |
| Asia/Shanghai | `E1-MINI-LOCAL-BUILD-20260809` | `node frontend/miniprogram/scripts/test-local-build.mjs` | pass. The test creates and removes only a system-temporary local DevTools copy, verifies local HTTP/HTTPS copy constraints and release-validator rejection of that copy, and confirms the tracked release config/project config remain unchanged. It is not a Developer Tools preview or upload. |
| Asia/Shanghai | `E1-WEB-01-LOCAL-20260809` | `cd frontend/web && npm run check`; includes typecheck, lint, policy, open-render, default-surface, and hostile-production built-worker suites | pass. Policy 18/18, default-surface 18/18, and hostile-production 18/18 passed; built-worker checks prove deferred routes return 404 and session/BFF routes 403 under unset/unknown runtime and hostile production flags. Public rendered HTML avoids deferred CTAs and unsafe Mini config; normal and image-worker responses carry the required security headers. |
| Asia/Shanghai | `E1-WEB-01-REVIEW-20260809` | independent source and built-worker review after unset-runtime fix | static/local PASS with no P0/P1: only explicit non-production modes open surfaces; default/unknown runtime is locked; CI includes both default-surface and hostile-production gates. No Cloudflare deployment, browser CSP execution, QR reachability, or remote CI run was performed. |
| Asia/Shanghai | `E1-VALIDATION-02-FINAL-20260809` | current-worktree `git diff --check`; documentation/candidate-package cross-check; independent core and evidence reviews | pass. All registered policy-independent code slices have automated local evidence; the Mini local-copy isolation test passed; candidate and G2 templates use the isolated E2E creator command and reject dirty/untracked candidate trees. The worktree remains intentionally dirty and unfrozen, so this is not same-SHA candidate, browser, Docker runtime, CI, staging, G1, or G2 evidence. |
| Asia/Shanghai | `E1-QA-02A-LOCAL-20260809` | `node --test scripts/candidate-evidence.test.mjs`; `node --check scripts/candidate-evidence.mjs`; `git diff --check`; `git diff --cached --check` | pass: native candidate-tool tests 10/10, zero skip/todo/cancelled; syntax and staged/unstaged whitespace checks passed. Independent review confirms fail-closed candidate-state, source/artifact symlink, gate-record/compare, sealed child-environment, inline/aggregate skip, and canonical external-SBOM behavior. This tests local tooling only; the current attached dirty worktree is correctly not a candidate and no evidence capture was generated. |
| Asia/Shanghai | `E1-QA-02A-GITLINK-20260809` | `git status --short`; `git diff --cached --name-status`; `git worktree list --porcelain` | verified: the future candidate index has only an index deletion for accidental unmapped `.worktrees/grok/task-f33b77`; the linked local worktree remains present at that path on branch `grok/task-f33b77`. No worktree was removed. The deletion takes effect only after a future commit and therefore is not current-HEAD candidate evidence. |
| Asia/Shanghai | `E1-WEB-02-LOCAL-20260809` | `cd frontend/web && npm run check` | pass: typecheck and lint passed; policy 18/18, open-render 18/18, default locked-worker 19/19, and hostile-production locked-worker 19/19 passed with zero skip/todo. The candidate check now explicitly asserts that both locked-worker runtime suites are invoked. This is a local build/worker result, not deployment or browser interaction evidence. |
| Asia/Shanghai | `E1-WEB-BROWSER-CAPABILITY-20260809` | local browser-interaction harness capability check | blocked locally: the required browser interaction module was unavailable after the local worker probe; no screenshots, keyboard/zoom, accessibility, performance, QR reachability, or real browser evidence was collected. This is a missing G1 local-environment capability, not a passed browser gate or an external result. |
| Asia/Shanghai | `E1-SHARED-02A-H-LOCAL-20260809` | `/bin/sh -n scripts/run-isolated-e2e.sh`; `npm run test:e2e:guard`; `npm run build`; `git diff --check` | pass: 43/43 native guard tests, zero skip/todo, passed shell syntax and API build. The runner now requires an explicit canonical execution-reference field before port/tool/workspace/Docker setup; the write-side ownership helper re-parses the raw environment before constructing PostgreSQL/Redis clients; raw Node entry and exported runner require the sealed POSIX-launch marker; the launcher rejects `NODE_OPTIONS`/`NODE_PATH` before creating its Node child and passes only its input allowlist. This is static/local safety evidence, not a Docker/DB/Redis runtime run. |
| Asia/Shanghai | `E1-SHARED-02A-H-DENY-20260809` | supported POSIX launcher with explicit local socket and Node path but no execution reference | expected safe failure before toolchain resolution, Docker, port allocation, database, or Redis action: `E2E_EXECUTION_AUTHORIZATION_EVIDENCE must be a canonical non-secret Evidence ID before isolated E2E may run`. The reference is only an auditable handle, not proof of an external authorization. |
| Asia/Shanghai | `E1-WEB-BROWSER-LOCAL-20260809` | built Worker at loopback plus real in-app browser: 5 public routes × 320/390/768/1440; route/header probes; click navigation; screenshots | partial local evidence with a blocking failure: public routes returned 200 and had main/h1/no horizontal overflow; deferred routes 404 and session/BFF probes 403; CSP/Permissions/Referrer/nosniff/XFO observed over HTTP. But `/_vinext/image?...` returned 400, public brand imagery had `naturalWidth=0`, and Worker logs showed undefined `ASSETS.fetch`; generated Worker config lacks assets/images binding. `WEB-03` is required before visual G1 evidence. HSTS/TLS, 200% zoom, reduced motion, complete keyboard sequence, aXe, QR reachability, remote CI, device, staging, and deployment remain unproven. This supersedes the earlier capability-only row only as a time-bounded local browser execution record; it does not make any external claim. |
| Asia/Shanghai | `E1-WEB-03-LOCAL-20260809` | `cd frontend/web && npm run check` with its loopback-only `test:worker-image-runtime` | pass: typecheck and lint passed; policy 18/18; open-render 23/23; default locked-worker 24/24; hostile-production locked-worker 24/24; and the built local Worker image suite 1/1, all zero skip/todo. The generated `wrangler.json` binds `ASSETS` and `IMAGES`; the test parses the public homepage's actual image URLs, covers 36/44/220/420/640 widths, and receives nonempty `200 image/png` responses without an optimizer error. The first-release default is safe static serving; image transformation stays opt-in until an external deployment binding is separately verified. No deploy, external image operation, or production request occurred. |
| Asia/Shanghai | `E1-WEB-03-REVIEW-20260809` | independent source/build/runtime review of Worker config, fallback, image widths, and loopback regression | pass: no in-scope P0/P1. The former missing-`ASSETS` runtime defect is closed. Fresh in-app-browser screenshot/natural-width, keyboard, zoom, reduced-motion, aXe, QR, TLS, remote CI, device, staging, and deployment evidence remain unproven; the unavailable browser surface is an outstanding G1 evidence gap rather than a pass. |
| Asia/Shanghai | `E1-QA-02B-LOCAL-20260809` | `node --check` for candidate/identity/CI-contract scripts; `node --test scripts/candidate-evidence.test.mjs scripts/verify-ci-candidate-identity.test.mjs scripts/g1-candidate-ci-contract.test.mjs`; YAML parse of API/Mini/Web/G1 workflows; `npm run test:e2e:guard`; Mini validate/direct-TSC/smoke/local-build; `cd frontend/web && npm run check`; `git diff --check` | pass: candidate/identity/CI native tests 14/14 with zero skip/todo; YAML and shell syntax passed; E2E guard 43/43 passed without Docker; Mini local gates passed with only the expected missing-external-AppID development warning; Web check passed including built loopback Worker suites (localhost-only bind authorization). Candidate capture v3 rejects legacy local E2E flags; no candidate capture, Docker/DB/Redis, remote CI, deployment, or external action ran. |
| Asia/Shanghai | `E1-QA-02B-REVIEW-20260809` | independent final static review of candidate tool, identity chain, workflows, generic CI permissions, and G2/candidate runbooks | pass: no in-scope P0/P1. Candidate CI fails closed unless a protected `refs/tags/...` ref, `GITHUB_REF_PROTECTED=true`, matching source/tree, and protected-environment approval reference are present; E2E jobs wait for tooling/API-static gates and run sequentially with a pinned preload-free Node path. This is source/contract evidence only; protected-environment configuration, remote CI, real E2E, candidate freeze, artifact/SBOM/migration/browser/device, G1, and G2 remain pending. |
| Asia/Shanghai | `E1-QA-02C-SBOM-LOCAL-20260809` | `node --check` for source-tree, candidate-evidence, SBOM, identity, and CI-contract scripts; `node --test scripts/candidate-evidence.test.mjs scripts/generate-candidate-sbom.test.mjs scripts/verify-ci-candidate-identity.test.mjs scripts/g1-candidate-ci-contract.test.mjs`; YAML parse for API/Mini/Web/G1 workflows; `git diff --check`; `git diff --cached --check` | pass: 21/21 native tests, zero skip/todo/cancelled. The deterministic lockfile-only generator reconstructs CycloneDX 1.6 from the three tracked npm v3 locks without `node_modules`, a package install, or network. It binds a canonical Git-mode source-tree SHA-256 rather than a 40-hex Git tree object ID; candidate finalization reconstitutes and compares the exact SBOM. YAML and whitespace checks passed. No candidate capture, remote CI, artifact upload, Docker, database, Redis, or migration ran. |
| Asia/Shanghai | `E1-QA-02C-SBOM-REVIEW-20260809` | two independent static reviews of SBOM/source-tree/protected-CI changes | pass: no in-scope P0/P1. Bundled packages retain parent-archive provenance without false child hashes; scopes distinguish required/optional/excluded dependencies; unsafe resolved URLs/output paths/preloads fail closed; candidate CI binds protected tag/ref, canonical source tree, and a separately configured `g1-candidate-sbom` approval reference before generation/upload. This is source/contract evidence only; protected-environment configuration, remote CI, immutable custody, real artifacts, migration, G1, and G2 remain pending. |
| Asia/Shanghai | `E1-QA-02C-MIGRATION-LOCAL-20260809` | `node --check backend/api/scripts/run-migration-compatibility.mjs`; `sh -n backend/api/scripts/run-migration-compatibility.sh`; `node --test backend/api/scripts/run-migration-compatibility.test.mjs`; `cd backend/api && npm run test:e2e:guard`; `npm run verify:prod-artifacts` | pass: migration native suite 22/22 and consolidated E2E-guard suite 65/65, zero skip/todo; syntax and production-artifact verification passed. The fresh-schema harness covers sealed launcher/Node digest refusal, artifact and source provenance, empty PostgreSQL/Redis stores, authenticated serial probes, owned-resource cleanup, and redacted receipts. No Docker, PostgreSQL, Redis, migration, image pull/build, candidate capture, CI, or external action ran. |
| Asia/Shanghai | `E1-QA-02C-MIGRATION-REVIEW-20260809` | two independent static reviews of migration launcher, runner, Compose, artifact boundary, cleanup, receipt, and runbooks | pass: no in-scope P0/P1 after final hardening. The result remains source/test evidence only: protected execution configuration, approved OCI artifacts, disposable Docker runtime, real forward migration, custody, rollback drill, G1, and G2 are still pending. |
| Asia/Shanghai | `E1-WEB-04-LOCAL-20260809` | `cd frontend/web && npm run test:policy`; `npm run check` | pass: policy 19/19; open-render 23/23; default locked-worker 24/24; hostile-production locked-worker 24/24; loopback Worker image runtime 1/1; all zero skip/todo. The resolver accepts only `weixin://dl/business/` with no query or one non-empty `t` token and falls back for hostile host/path/query/port/credential/fragment values. The runtime test bound only a local loopback Worker; no deployment or WeChat-client action occurred. |
| Asia/Shanghai | `E1-WEB-04-REVIEW-20260809` | independent source/security review of Mini Program deep-link resolver and CTA call path | pass: no in-scope P0/P1. The CTA renders only resolver-approved links; protocol, exact host, exact path, query grammar, credentials, ports, and fragments are fail-closed. A deployed/WeChat-client deep-link result is still not claimed. |
| Asia/Shanghai | `E1-WEB-05-LOCAL-20260809` | `cd frontend/web && npm run check`; rebuilt `vinext start --ip 127.0.0.1 --port 3310`; loopback `curl` for `/` | pass: typecheck and lint passed; policy 19/19; open-render 24/24; default locked-worker 25/25; hostile-production locked-worker 25/25; worker-image runtime 1/1; all zero skip/todo. The new built-worker test calls the Worker with an absent env object and receives public HTML. The rebuilt local production adapter served `/` as `200 text/html`; CSP, Permissions-Policy, Referrer-Policy, `X-Content-Type-Options: nosniff`, and `X-Frame-Options: DENY` remained present. The listener was local only; no external system was contacted. |
| Asia/Shanghai | `E1-WEB-05-BROWSER-BOUNDARY-20260809` | retry only through the supported in-app browser bridge after the local Worker probe | blocked locally: the supported browser surface could not connect to the host loopback listener, so no fresh screenshot, DOM, keyboard, zoom, reduced-motion, aXe, QR, device, or deployment evidence was collected. This does not invalidate the local Worker response proof and does not count as a browser pass. |
| Asia/Shanghai | `E1-SHARED-02DRBR-LOCAL-20260810` | targeted payments/orders/attendance Jest suites; full `npm test -- --runInBand`; `npm run build`; Mini strict TypeScript; `node scripts/openapi-controller-contract.test.mjs`; staged/unstaged `git diff --check` | pass: targeted 80 tests; full API unit suites 144/144 and tests 1330/1330; API build and Mini strict TypeScript passed; OpenAPI 16/16 passed; whitespace checks passed. Customer refund-sync returns the customer projection after an owner-scoped lookup, attendance text-only projection preserves text with `attachments=[]` and an empty summary before `groupBy`, and participant/staff/claimable contracts are exact. No Docker/DB/Redis E2E, candidate capture, CI, staging, or external action ran. |
| Asia/Shanghai | `E1-SHARED-02DRBR-REVIEW-20260810` | independent post-fix source/contract review and 5 focused Jest suites | pass: P0=0/P1=0. Staff and claimable routes use exact schemas distinct from participant view; staff assignment IDs are nullable and the safe refund projection is shared; non-owner refund sync is non-probing and never queries the provider; customer, companion, and staff text-only reads retain statement text while hiding attachments and avoid historical TRTC queries. This is not runtime E2E or a G1 release result. |
| Asia/Shanghai | `E1-QA-02D-LOCAL-20260810` | `env -u NODE_OPTIONS -u NODE_PATH node --test scripts/candidate-evidence.test.mjs scripts/candidate-input-policy.test.mjs scripts/generate-candidate-sbom.test.mjs scripts/verify-ci-candidate-identity.test.mjs scripts/g1-candidate-ci-contract.test.mjs`; `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH npm run test:e2e:guard`; `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH npm run test:preflight:static`; `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH npx tsc -p tsconfig.json --noEmit` | pass: candidate tooling 27/27, API E2E guard 75/75, static preflight 71/71, and strict TypeScript all passed with zero skip/todo/pending/cancelled. This verified source/test/contract behavior only. No Docker, database, Redis, network dependency audit, candidate capture, remote CI, deployment, upload, or external action ran. |
| Asia/Shanghai | `E1-QA-02D-REVIEW-20260810` | independent final static audit of candidate input policy, G1 CI contract, zero-skip/E2E boundaries, and runbook truthfulness | pass: P0=0/P1=0 after the QA-02D ledger correction. Candidate-input policy, dependency-audit, sealed PostgreSQL preflight, digest-pinned/no-pull E2E contract, zero-skip wrappers, and reference-only runbook boundaries are source/contract verified. Protected-environment configuration, Docker runtime, actual dependency audit, remote CI, frozen candidate, artifact custody, browser/device, staging, G1, and G2 remain unproven. |
| Asia/Shanghai | `E1-QA-02D-OPS-LOCAL-20260810` | `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH node --test scripts/smoke-authorization.test.mjs`; `npm run test:e2e:guard`; `/bin/sh -n scripts/production-smoke.sh`; `/bin/sh -n scripts/acceptance-smoke.sh` | pass: 4 focused smoke-authorization tests and consolidated guard 79/79, zero skip/todo/pending/cancelled. Missing/expired/mismatched production records and non-loopback/mock acceptance invocations stop before fake `curl` can run; production smoke no longer contains POST probes and asserts `appEnv=production`. No Docker, database, Redis, remote API, candidate capture, CI, deployment, upload, or external action ran. |
| Asia/Shanghai | `E1-QA-02D-OPS-LOCAL-RERUN-20260810` | `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH node --test scripts/smoke-authorization.test.mjs`; `npm run test:e2e:guard`; `npm run test:preflight:static`; `npx tsc -p tsconfig.json --noEmit`; shell syntax and staged/unstaged whitespace checks | pass: focused smoke authorization 4/4, consolidated guard 79/79, static preflight 71/71, and strict TypeScript passed with zero skip/todo/pending/cancelled. The authorization test binds only a temporary `127.0.0.1` observer and proves missing, expired, and mismatched production records establish zero connections before the trusted curl binary can run; every production curl invocation uses `-q` first to disable ambient curl configuration. No Docker, database, Redis, remote API, candidate capture, CI, deployment, upload, or external action ran. |
| Asia/Shanghai | `E1-QA-02D-OPS-LOCAL-FINAL-20260810` | `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH npm run test:e2e:guard`; `npm run test:preflight:static`; `npx tsc -p tsconfig.json --noEmit`; shell syntax and staged/unstaged whitespace checks | pass: consolidated guard 79/79, static preflight 71/71, and strict TypeScript passed with zero skip/todo/pending/cancelled. This supersedes the earlier fake-curl observation as pre-request proof: both smoke scripts now use an actual temporary `127.0.0.1` observer for refusal-path assertions; production requires a scoped record before its first connection, while mock acceptance permits only literal `http://127.0.0.1[:port]`, uses trusted curl, disables curl configuration/proxies, and requires explicit local intent. No Docker, database, Redis, remote API, candidate capture, CI, deployment, upload, or external action ran. |
| Asia/Shanghai | `E1-QA-02D-OPS-REVIEW-20260810` | independent current-disk review after final numeric-loopback and curl-configuration hardening | pass: P0=0/P1=0. The production script validates record/target/expiry before any request, is read-only, uses trusted curl with `-q --noproxy`, and asserts `appEnv=production`; the mock acceptance script rejects every target except literal `http://127.0.0.1[:port]` before curl initialization and its sole request path has the same trusted-curl/config/proxy boundary. Current operational docs are controlled-reference only. This is static/local review; no remote API, Docker, database, Redis, CI, deployment, upload, or external action ran. |
| Asia/Shanghai | `E1-SHARED-02G-LOCAL-20260810` | `cd backend/api && npm test -- --runInBand src/payments/payments.service.spec.ts src/recommendations/recommendations.service.spec.ts src/config/configuration.spec.ts`; `npx tsc -p tsconfig.json --noEmit --incremental false`; `node --test scripts/deployment-preflight.test.mjs`; staged/unstaged `git diff --check` | pass: targeted API suites 3/3 and 122/122 tests; strict TypeScript passed with incremental cache disabled; deployment preflight 20/20; zero skips/todo/cancelled and whitespace checks passed. Customer refund preserves the preprojected safe DTO; staging/production first-release settings are hard-locked to text-only and personalization-off; no Docker, database, Redis, remote CI, deployment, or external action ran. |
| Asia/Shanghai | `E1-SHARED-02G-REVIEW-20260810` | independent current-disk review of customer-refund DTO flow, first-release configuration/preflight, recommendation reads/ranking/writes, and focused local tests | pass: P0=0/P1=0. The refund wrapper no longer re-projects ISO-string dates; configuration/preflight fail closed in staging/production; raw environment values cannot re-enable behavioural/order personalization. `PERSONALIZATION-R01` historical-consent and future-enable policy remains blocked. This is static/local source evidence only; no Docker, database, Redis, CI, deployment, or external action ran. |
| Asia/Shanghai | `E1-QA-02E-A-RETRACTION-20260810` | independent trust-boundary review of the former in-repository candidate CI/E2E design | retracted as a usable candidate-CI/E2E result: a candidate checkout supplied the workflow/control code and had host-Docker reachability before claimed admission. Earlier structural checks remain historical local evidence only; no CI, Docker, database, Redis, or external action ran. |
| Asia/Shanghai | `E1-QA-02E-A-R-LOCAL-20260810` | `env -u NODE_OPTIONS -u NODE_PATH node --test scripts/g1-candidate-ci-contract.test.mjs`; Ruby YAML parse of `.github/workflows/g1-candidate.yml` and `g1-candidate-control-plane.yml`; `git diff --check`; `git diff --cached --check`; `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH npm run test:e2e:guard`; `npm run test:preflight:static` | pass: control-plane contract 4/4, API guard 85/85, and static preflight 73/73, all zero skip/todo/pending/cancelled; YAML and whitespace checks passed. The smoke guard used only a temporary local `127.0.0.1` observer. No Docker, database, Redis, remote CI, upload, deployment, or external request ran. |
| Asia/Shanghai | `E1-QA-02E-A-R-REVIEW-20260810` | independent current-disk review of source-only candidate workflow downgrade, external control-plane template, authorization resolver binding, socketless executor/harness provenance, and `always()` admission/denial receipt | pass: P0=0/P1=0 at static/local source-contract scope. Candidate source no longer supplies a privileged in-repository Docker path; the future external controller binds candidate repository/SHA/tree, authorization register, executor image/harness, and admission/denial state independently of candidate output. A separate protected control repository/ref, registry, runner ACL, isolation attestation, and a real authorized run remain unverified external G1 conditions. |
| Asia/Shanghai | `E1-QA-02E-B-A-LOCAL-20260810` | `env -u NODE_OPTIONS -u NODE_PATH node --test scripts/g1-candidate-ci-contract.test.mjs backend/api/scripts/ci-e2e-safe-runtime.test.mjs`; Ruby YAML parse of API/G1/control-plane/iOS/Mini/Web workflows; `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH npm run test:e2e:guard && npm run test:preflight:static`; staged/unstaged `git diff --check` | pass: metadata-hygiene contracts 7/7, API safety guard 85/85, and static preflight 73/73, all with zero skip/todo/pending/cancelled; YAML and whitespace checks passed. The guard used only mock command injection and a temporary `127.0.0.1` smoke observer. No CI dispatch, Docker, database, Redis, migration, browser, registry, remote request, upload, deployment, or external write ran. |
| Asia/Shanghai | `E1-QA-02E-B-A-REVIEW-20260810` | independent current-disk workflow/control-contract review plus in-memory block- and flow-style mutation checks | pass: P0=0/P1=0 at static/local metadata-hygiene scope. API/G1/iOS/Mini/Web ordinary workflows contain exactly one pinned credentialless/submoduleless/LFSless checkout and one fixed sealed Git metadata body; they do not interpret candidate code or expose service/DB/Redis/E2E/Docker/OCI/artifact/receipt capability. This does not make them candidate CI, G1, or G2 evidence; a separate protected control repository/ref, authorization register, socketless executor, runner/mount/token/command-file/egress attestation, and real authorized execution remain external blockers. |
| Asia/Shanghai | `E1-QA-02E-B-B-LOCAL-20260810` | `env -u NODE_OPTIONS -u NODE_PATH node --test scripts/g2-browser-evidence-card-contract.test.mjs scripts/oci-builder-custody-contract.test.mjs scripts/g1-candidate-ci-contract.test.mjs backend/api/scripts/run-migration-compatibility.test.mjs`; `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH npm run test:e2e:guard && npm run test:preflight:static`; staged/unstaged `git diff --check` | pass: focused contract suite 46/46, API guard 103/103, and static preflight 73/73, all zero skip/todo/pending/cancelled. Browser cards reject repository-local/symlink/nonregular/malformed evidence; passed cards must meet approved thresholds/minimum samples, keep every interaction facet passed, and report zero aXe blocking violations. OCI receipts bind Dockerfile labels and distinct issuer/reviewer; the migration tool is local-operator-only. The smoke guard used only a temporary local `127.0.0.1` observer. No browser/device, Docker, database, Redis, migration, CI, registry, upload, deployment, or external request ran. |
| Asia/Shanghai | `E1-QA-02E-B-B-REVIEW-20260810` | two independent current-disk reviews of browser-card and OCI/migration-custody structural validators | pass: P0=0/P1=0 at static/local structural-contract scope. The browser validator is read-only and requires the full public route/viewport/accessibility/performance/cleanup schema; the OCI validator is read-only, locks its labels to `backend/api/Dockerfile`, and rejects self-review. Both documents accurately retain their structural-only limitation. External browser execution, builder/custody, migration, protected control-plane administration, and all G1/G2 runtime evidence remain pending. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-01-LOCAL-20260810` | `env -u NODE_OPTIONS -u NODE_PATH node --test scripts/g2-browser-evidence-card-contract.test.mjs`; `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH node --test scripts/openapi-controller-contract.test.mjs`; `./node_modules/.bin/jest src/orders/orders.service.spec.ts --runInBand`; `npm test -- --runInBand src/payments/payments.service.spec.ts`; `cd frontend/miniprogram && npx tsc --noEmit`; staged/unstaged `git diff --check` | pass: browser-card contract 11/11, OpenAPI controller contract 17/17, order service 87/87, payment service 57/57, and Mini strict type check passed with zero skips/todo/cancelled. Customer order projection and both refund paths omit settlement/assurance facts; passed cards require complete chronological cleanup and explicit frozen-candidate/artifact bindings. No browser/device, Docker, database, Redis, CI, registry, staging, deployment, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-01-REVIEW-20260810` | independent current-disk reviews of the five backend/contract/Mini files and the browser-card validator/docs | pass: P0=0/P1=0 at static/local scope. The generic order serializer, customer participant paths, refund create/sync, OpenAPI, and Mini model agree on the safe field boundary; browser-card CLI rejects a passed record unless all four expected bindings and completed chronological cleanup are present. This is not browser/device, candidate, custody, G1, or G2 runtime evidence. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-02-LOCAL-20260810` | `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH ./node_modules/.bin/jest --runInBand src/orders/orders.service.spec.ts src/community/community.service.spec.ts`; `node scripts/openapi-controller-contract.test.mjs`; `env -u NODE_OPTIONS -u NODE_PATH node --test scripts/oci-builder-custody-contract.test.mjs scripts/g2-browser-evidence-card-contract.test.mjs`; static Markdown checks for four complete authorization rows, 13 scenario cards, and the six OCI expected-binding flags; staged/unstaged `git diff --check` | pass: order/community 103/103, OpenAPI contract 18/18, OCI/browser structural contracts 21/21, all zero skip/todo/cancelled. The reschedule responder view and legacy community cover URL are fail closed; passed OCI receipt validation requires independent candidate/source/artifact inputs; G2 action rows remain pending/blocked. No browser/device, Docker, database, Redis, CI, registry, staging, migration, rollback, deployment, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-02-REVIEW-20260810` | independent current-disk API/OpenAPI review and independent OCI/G2-package review | pass: P0=0/P1=0 at static/local scope. Companion reschedule acceptance cannot expose customer refund/feedback material; list/like return null for historical cover URLs; strict response and OCI CLI contracts resist projection or same-SHA substitution regressions. The G2 authorization table accurately separates browser, OCI custody, external migration, and restore/rollback actions over 13 cards. This is not G1/G2 runtime, custody, browser, migration, or authorization evidence. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-03-LOCAL-20260810` | `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH ./node_modules/.bin/jest src/companions/companions.service.spec.ts --runInBand`; `env -u NODE_OPTIONS -u NODE_PATH node frontend/miniprogram/scripts/smoke.mjs`; `env -u NODE_OPTIONS -u NODE_PATH node backend/api/node_modules/typescript/lib/tsc.js -p frontend/miniprogram/tsconfig.json --noEmit`; `env -u NODE_OPTIONS -u NODE_PATH node --test scripts/oci-builder-custody-contract.test.mjs`; staged/unstaged `git diff --check` | pass: companion service 57/57 and OCI custody contract 11/11, with Mini runtime smoke and strict Mini TypeScript passing; no skipped/todo/cancelled test result. Historical inactive voice SKU activation is refused before API mutation and before Mini modal/PATCH; passed OCI receipts require all seven independent CLI bindings, including build-context tree. The candidate commit/freeze and earnings documents are local policy-record templates only. No browser/device, Docker, database, Redis, CI, registry, staging, migration, rollback, candidate commit, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-03-REVIEW-20260810` | three independent current-disk reviews: API/Mini historical voice SKU activation; OCI seventh-binding implementation and all three execution references; authority/ledger/freeze-record and earnings-decision wording | pass: P0=0/P1=0 at static/local scope. The API remains authoritative while the Mini provides a non-bypassable UX guard; OCI passed-path commands bind candidate/artifact plus scoped build context independently; the local `CANDIDATE-COMMIT-R01` record is precise and separate from protected tag/ref authority; `EARNINGS-R01` is companion-safe but remains undecided. G1 remains NO-GO, G2-ready NO-GO, and G2 BLOCKED; no external fact was created or asserted. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-04-LOCAL-20260810` | `env -u NODE_OPTIONS -u NODE_PATH node --test scripts/candidate-evidence.test.mjs scripts/g2-browser-evidence-card-contract.test.mjs`; staged/unstaged `git diff --check` | pass: candidate-evidence and browser-card structural suites 25/25 with zero skip/todo/cancelled. A clean detached candidate omission of each newly required browser-card, OCI-custody, external control-plane, Dockerfile, and local migration contract path is rejected before capture; every card outcome requires exact ordered serious/critical aXe blocking severities. No candidate capture, browser/device, Docker, database, Redis, CI, registry, staging, migration, rollback, commit, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-04-REVIEW-20260810` | independent current-disk reviews of candidate required-source omissions, fixed browser aXe policy, documentation, and state boundary | pass: P0=0/P1=0 at static/local source-contract scope. The candidate cannot merely hash an incomplete evidence package, and a card author cannot redefine the serious/critical accessibility blocker. This is not browser/device, custody, candidate, CI, G1, or G2 runtime evidence; all external and product gates remain pending/blocked. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-05-LOCAL-20260810` | `env -u NODE_OPTIONS -u NODE_PATH node --test scripts/candidate-input-policy.test.mjs scripts/generate-candidate-sbom.test.mjs scripts/candidate-evidence.test.mjs scripts/verify-ci-candidate-identity.test.mjs scripts/g2-browser-evidence-card-contract.test.mjs`; syntax checks for the four candidate/SBOM/browser scripts; staged/unstaged `git diff --check` | pass: 39/39, zero fail/skip/todo/cancelled (candidate evidence 14, input policy 4, SBOM 6, CI identity 3, current browser contract 12). All candidate private/package-manager denylist names fail closed under ASCII case variation; API/Mini/Web SBOM generation rejects shrinkwrap case variants before it creates an output parent. This broad rerun includes the current 12-case browser contract; it does not rewrite historical F04 25/25 evidence. No dependency install, dependency resolution/audit, registry access, real candidate capture, browser/device, Docker, database, Redis, CI, staging, migration, rollback, commit, or external action ran; existing toolchain metadata read only a trusted local npm version. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-05-REVIEW-20260810` | independent current-disk review of case-folded candidate policy, three-root SBOM denial/atomicity, detached fixture coverage, documentation, and ledger boundary | pass: P0=0/P1=0 at static/local source-contract scope. Mixed-case candidate private/package-manager inputs cannot exploit a case-folding filesystem, and a case-sensitive executor cannot bypass the SBOM lock boundary with an alternate-cased shrinkwrap. This is not candidate, custody, browser/device, CI, G1, or G2 runtime evidence; all product and external gates remain pending/blocked. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-06-LOCAL-20260810` | `env -u NODE_OPTIONS -u NODE_PATH node --test scripts/candidate-evidence.test.mjs`; `node --check scripts/candidate-evidence.mjs`; staged/unstaged `git diff --check` | pass: candidate-evidence 15/15 with zero fail/skip/todo/cancelled. A clean detached candidate with a tracked child below a required-file path, or a regular file at the required migration-directory path, is rejected before capture output preparation; the source registry requires each path's declared non-symlink kind. No candidate capture, commit, browser/device, Docker, database, Redis, CI, registry, migration, staging, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-06-REVIEW-20260810` | independent current-disk candidate-evidence/input-policy review plus independent rerun of candidate-evidence 15/15 and input-policy 4/4 | pass: P0=0/P1=0 at static/local source-contract scope. Git tracked-path matching can no longer turn a tracked descendant into a substitute for a required file; the only declared directory is non-symlink migrations. This is not candidate, custody, CI, G1, or G2 runtime evidence. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-07-LOCAL-20260810` | `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH ./node_modules/.bin/jest src/orders/orders.service.spec.ts --runInBand`; `env -u NODE_OPTIONS -u NODE_PATH node scripts/openapi-controller-contract.test.mjs`; staged/unstaged `git diff --check` | pass: orders 89/89 and OpenAPI contract 19/19, zero fail/skip/todo/cancelled. Historical voice orders under text-only cannot create, expire, or accept a reschedule proposal before the existing 422 guard; regressions assert no proposal/order/timeline/notification/audit mutation. No database, Redis, Docker, CI, candidate, browser/device, staging, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-07-REVIEW-20260810` | independent current-disk review and rerun of orders 89/89 plus OpenAPI 19/19 | pass: P0=0/P1=0 at static/local server/API-contract scope. Request and acceptance guard placement is after authorization/read binding but before every persistent reschedule side effect, including expired-proposal update; both POST routes declare exact 422 ErrorEnvelope behavior. This is not a real order action, candidate, CI, G1, or G2 result. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-08-DISCOVERY-20260811` | `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH npm test`; direct Jest JSON result plus a replay of the current zero-skip parser | local P1 confirmed: Jest itself reported 144/144 suites and 1337/1337 passed with `numPendingTests=0` and `numTodoTests=0`, but the broad parser counted ordinary log text `2 skipped` and `4 skipped` as six test outcomes. No test skip was accepted, and no Docker, database, Redis, CI, browser, registry, candidate, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-08-LOCAL-20260811` | `cd backend/api && env -u NODE_OPTIONS -u NODE_PATH node --check scripts/assert-zero-skips.mjs`; `node --test scripts/assert-zero-skips.test.mjs`; `npm test`; `npm run test:preflight:static`; staged/unstaged `git diff --check` | pass: parser native suite 4/4, exact wrapped API unit gate 144/144 suites / 1337/1337 tests, and static preflight 76/76, all zero skipped/todo/pending/cancelled. Formal Jest/TAP summaries with nonzero outcomes still reject; business diagnostics carrying `skipped` counts do not. This is local source/test evidence awaiting independent review, not a candidate, CI, G1, or G2 result; Prisma client generation remained local and no Docker, database, Redis, browser, registry, CI, candidate, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-08-REVIEW-20260811` | independent source/docs review; rerun `node --check scripts/assert-zero-skips.mjs`, `node --test scripts/assert-zero-skips.test.mjs`, exact `npm test`, a real all-skipped filtered Jest path through the wrapper, and a real Node TAP skip fixture; staged/unstaged `git diff --check` | pass: P0=0/P1=0 at static/local scope. Formal Jest `Tests:`/`Test Suites:` total lines and TAP summaries/inline markers retain skip rejection; routine `2 skipped` / `4 skipped` diagnostics are not test outcomes. Native suite 4/4 and exact unit gate 144/144 suites / 1337/1337 tests pass; the all-skipped Jest run rejects 1 suite plus 89 tests and TAP skip rejects. This is not candidate, CI, G1, or G2 evidence; no Docker, database, Redis, browser, registry, CI, candidate, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-09-DISCOVERY-20260811` | independent current-disk candidate-evidence audit; direct replay of `skippedCount('payment reconciliation: 2 skipped provider images\\n')` | local P1 confirmed: the candidate gate parser returned `2` for ordinary diagnostic text, so a successful future `API_UNIT` record could fail before candidate finalization. The correction must preserve zero-skip rejection under formal Jest/TAP grammar. No candidate capture, commit, package install, Docker, database, Redis, browser, CI, registry, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-10-DISCOVERY-20260811` | independent current-disk Mini order list/detail and smoke audit | local P1 confirmed: a historical voice order under the first-release text-only lock still displayed and submitted confirm/start/create-reschedule/accept-reschedule actions even though the API already returns `COMMERCIAL_SURFACE_TEXT_ONLY` / 422 before writes. This is a local UX/contract gap, not a data-integrity bypass; its correction must preserve read, cancellation/refund/support and closure paths. No Mini device, candidate, CI, Docker, database, Redis, browser, registry, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-11-DISCOVERY-20260811` | independent current-disk replay using real Node and Jest console diagnostics | local P1 confirmed: both F08's API wrapper and F09's candidate parser counted real `console.log('skipped 2')` / `console.log('Tests: 2 skipped, 2 total')` output as test outcomes despite exit zero and a real final zero summary. F09's first refinement is superseded; a complete terminal-summary grammar is required without accepting any actual skipped/todo/pending/cancelled result. No candidate capture, commit, package install, Docker, database, Redis, browser, CI, registry, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-12-DISCOVERY-20260811` | independent historical-package entry-point audit | local P1 confirmed: five 2026-08-04 package entry files opened with historical “Implementation complete” / Go claims attached to dirty `main@9cf5e3849a9654ddfddb8046bf29a580533fa268` and lacked a visible current-state warning. This documentation correction preserves historical content while adding a non-destructive supersession boundary. No candidate, commit, CI, Docker, database, Redis, browser, registry, deployment, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-10-LOCAL-20260811` | `env -u NODE_OPTIONS -u NODE_PATH backend/api/node_modules/.bin/tsc -p frontend/miniprogram/tsconfig.json --noEmit`; `node frontend/miniprogram/scripts/validate.mjs`; `node frontend/miniprogram/scripts/smoke.mjs`; scoped `git diff --check` | pass: strict Mini TypeScript, 31-page/5-tab structure validation, and runtime smoke with 859 mock API calls. Historical voice orders under release text-only hide and handler-block confirm/start/create-reschedule/accept-reschedule in detail and list; the paid list start direct-call emits no POST. Timeline reads, rejection/closure, cancellation/refund/support, and already in-service completion remain available. The AppID warning is the expected local-development boundary. No device, candidate, CI, Docker, database, Redis, browser, registry, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-10-REVIEW-20260811` | independent current-disk Mini order detail/list source and smoke review; rerun strict TypeScript, structure validation, runtime smoke, and scoped `git diff --check` | pass: P0=0/P1=0 at static/local scope. Rendering conditions and handlers provide two independent guards for each unavailable future-fulfilment action; the paid list start regression is explicitly covered, and permitted closure paths remain intact. This is not Mini device, candidate, G1, or G2 evidence. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-11-LOCAL-20260811` | `node --check scripts/candidate-evidence.mjs`; `node --check backend/api/scripts/assert-zero-skips.mjs`; `env -u NODE_OPTIONS -u NODE_PATH node --test scripts/candidate-evidence.test.mjs backend/api/scripts/assert-zero-skips.test.mjs`; exact wrapped all-skipped Jest rejection; local `backend/api npm test`; staged/unstaged `git diff --check` | pass: focused parser suites 21/21 with zero skipped/todo/cancelled. Complete terminal Jest/TAP summaries retain fail-closed counting; real Node/Jest console diagnostics resembling result lines count zero; the actual filtered all-skipped Jest path rejects 1 suite plus 89 tests. No test subject or package script changed, and no candidate, Docker, database, Redis, browser, CI, registry, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-11-REVIEW-20260811` | independent current-disk source review and rerun of API parser 5/5 plus candidate-evidence 16/16; real Node/Jest diagnostic and skip/todo fixtures; syntax and diff checks | pass: P0=0/P1=0 at static/local scope. Only complete terminal summary blocks or formal inline TAP skip/TODO records contribute counts; aggregate and inline TAP forms do not double count; ANSI-decorated formal results still reject. The earlier F08/F09 grammars remain historical/superseded rather than silently rewritten. This is not candidate, CI, G1, or G2 evidence. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-12-LOCAL-20260811` | compare the first lines of all five 2026-08-04 package entry files; verify the relative current-state link; scoped `git diff --check` | pass: 5/5 entry files begin with the identical historical/superseded banner before any old Go/complete heading; the link resolves to the current remediation state; each file changes only by the two-line banner and retains its historical body/G0 decisions. No candidate, commit, CI, deployment, or external action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-12-REVIEW-20260811` | independent current-disk review of all five historical entry files, link target, current gate state, and scoped diff | pass: P0=0/P1=0 at static/local documentation scope. No historical title can precede the warning, and the linked authoritative state remains G1 NO-GO / G2-ready NO-GO / G2 BLOCKED. This does not create current candidate, authorization, release, or external evidence. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-13-LOCAL-20260811` | `USER-AUTH-ALL-20260811`; focused identity/recommendation/earnings/payment-dispute tests; `backend/api npm test`; `npm run test:e2e:guard`; `npm run test:preflight:static`; `npm run verify:prod-artifacts`; OpenAPI and deployment-preflight native suites; all eight root candidate-tool suites; Mini strict TypeScript/validate/smoke/local-build isolation; Web `npm run check:candidate`; staged/unstaged diff checks | pass at local owner scope: API 144/144 suites and 1342/1342 tests; E2E guard 110/110; static preflight 78/78; root candidate tooling 61/61; OpenAPI 20/20; deployment preflight 21/21; Mini 31 pages/5 tabs and 859 mock API calls plus local-copy isolation; complete Web candidate check including 19/19 policy and 1/1 built Worker runtime. All recorded test summaries have zero fail/skip/todo/cancelled. The three loopback smoke/Worker checks required sandbox-external localhost permission but contacted no remote target. Product choices are implemented fail closed. No Docker, database, Redis, registry, CI, staging, provider, device, deployment, or real-money action ran. |
| Asia/Shanghai | `E1-G1-FOLLOWUP-13-OWNER-REVIEW-20260811` | main-owner current-disk source/contract review plus complete local gate rerun | owner review pass: no remaining local P0/P1 was found in the authorized identity, personalization, companion-earning, or payment-dispute-media closure. Downstream moderation tests bypass the frozen identity gate only inside explicitly labelled unit scope; real entry-point tests reject both false and legacy true identity values before writes. This is deliberately **not** an independent review and cannot satisfy the candidate/G1 reviewer row. The worktree is not evidence until one clean candidate commit and detached capture exist. |

No external services, deployments, uploads, database migrations, Redis flushes, or production actions were executed by this recovery task.
